apiVersion: skaffold/v4beta14
kind: Config

metadata:
  name: ci-cd-practice

build:
  artifacts:
    - image: app
      docker:
        dockerfile: Dockerfile

manifests:
  rawYaml:
    - k8s/deployment.yaml

profiles:
  - name: dev
